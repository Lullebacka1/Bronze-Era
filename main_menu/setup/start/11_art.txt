work_of_art_manager = {
	# Bronze Era uses positive engine dates. These monuments are already old
	# at the visual 1209 BC start, so they are anchored to the stable start date.
	monument = { location = amesbury origin = amesbury  quality = 10 creation_date = 1.1.1  key = stonehenge }
}
