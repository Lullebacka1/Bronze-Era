# Bronze Era override.
# The vanilla file checks many medieval special unit types that are absent from
# the total conversion, producing thousands of script errors. Keep the same
# public custom localization keys, but use generic fallbacks only.

subunit_type_army_infantry = {
	type = sub_unit

	text = {
		localization_key = subunit_type_army_infantry
		fallback = yes
	}
	log_loc_errors = no
	if_invalid_loc = fallback_to_next_entry
}

subunit_type_army_cavalry = {
	type = sub_unit

	text = {
		localization_key = subunit_type_army_cavalry
		fallback = yes
	}
	log_loc_errors = no
	if_invalid_loc = fallback_to_next_entry
}

subunit_type_army_artillery = {
	type = sub_unit

	text = {
		localization_key = subunit_type_army_artillery
		fallback = yes
	}
	log_loc_errors = no
	if_invalid_loc = fallback_to_next_entry
}

subunit_type_army_auxiliary = {
	type = sub_unit

	text = {
		localization_key = subunit_type_army_auxiliary
		fallback = yes
	}
	log_loc_errors = no
	if_invalid_loc = fallback_to_next_entry
}
