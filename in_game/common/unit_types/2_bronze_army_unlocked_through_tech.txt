a_slingers = {
	copy_from = a_bronze_age_1_traditions_light_infantry
	
	strength_damage_done = -0.02

	upgrades_to = a_archers

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = slinger_infantry_maintenance
	construction_demand = slinger_infantry_construction
	gfx_tags = { slingers_tag medium_tag }
}

a_simple_chariots = {
	copy_from = a_age_1_traditions_light_chariot
	
	
	upgrades_to = a_heavy_chariots

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.2
	}
	
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.02
	}
	gfx_tags = { medium_tag }
}

a_heavy_chariots = {
	copy_from = a_age_1_traditions_heavy_chariot

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.1
	}
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.01
	}

	upgrades_to = a_heavy_cavalrymen

	gfx_tags = { heavy_tag }
}

a_light_chariots = {
	copy_from = a_age_1_traditions_light_chariot
	
	
	upgrades_to = a_heavy_chariots

	strength_damage_done = 0.02

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.2
	}
	
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.02
	}
	gfx_tags = { medium_tag }
}

a_spearmen = {
	copy_from = a_bronze_age_1_traditions_heavy_infantry

	upgrades_to = a_footmen

	strength_damage_taken = -0.02

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = spearmen_infantry_maintenance
	construction_demand = spearmen_infantry_construction
}

a_axemen = {
	copy_from = a_bronze_age_1_traditions_heavy_infantry

	upgrades_to = a_footmen

	strength_damage_taken = 0.02
	strength_damage_done = 0.05

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = axemen_infantry_maintenance
	construction_demand = axemen_infantry_construction
}

a_swordsmen = {
	copy_from = a_bronze_age_1_traditions_heavy_infantry

	upgrades_to = a_footmen

	strength_damage_done = 0.02
	initiative = 1

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = swordsmen_infantry_maintenance
	construction_demand = swordsmen_infantry_construction
}

a_neolithic_bowmen = {
	copy_from = a_bronze_age_1_traditions_light_infantry

	upgrades_to = a_composite_bowmen

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.05
	}

	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = neolithic_bowmen_maintenance
	construction_demand = neolithic_bowmen_construction
	gfx_tags = { neolithic_bowmen_tag medium_tag }
}

a_composite_bowmen = {
	copy_from = a_bronze_age_1_traditions_light_infantry

	upgrades_to = a_footmen

	initiative = 1

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = composite_bowmen_maintenance
	construction_demand = composite_bowmen_construction
	gfx_tags = { composite_bowmen_tag medium_tag }
}

a_mustered_infantry = {
	copy_from = a_age_1_traditions_light_infantry

	buildable = no
	levy = yes

	gfx_tags = { light_tag }
}