measles = {
	# Bronze Era: disabled.
	# Keep the vanilla key inert so vanilla references do not break, while the
	# active Bronze Age disease set stays limited to vanilla-compatible entries.
	potential = { always = no }

	monthly_spawn_chance = { value = 0 }
	spawn = { }
	r0 = { value = 0 }
	calc_interval_days = { 365 365 }
	location_stagnation_chance = { value = 1 }
	sub_unit_stagnation_chance = { value = 1 }
	percentage_to_meet_their_fate_on_calc = { value = 0 }
	location_modifier = { }
	monthly_resistance_reduction = 0
	mortality_rate = { value = 0 }
	character_mortality_chance = { value = 0 }
	on_spread_to_country = { }

	map_color = {
		lerp = {
			min_color = rgb { 244 187 119 }
			max_color = rgb { 252 136 0 }
			factor = "disease_presence(scope:disease)"
		}
	}

	secondary_map_color = {
		if = {
			limit = {
				disease_affects_pops_here = scope:disease
			}
			value = rgb { 100 120 188 }
		}
	}

	location_spread_threshold = { value = 1 }
}
