# adds untiy with the $value$ its set in the events file
change_mycenaean_league_authority = {
	international_organization:mycenaean_league ?= {
		custom_description = {
			text = change_mycenaean_league_authority_text
			value = $value$
			change_variable = {
				name = mycenaean_league_authority
				add = $value$
			}
			if = {
				limit = { has_variable = mycenaean_league_authority }
				if = {
					limit = { var:mycenaean_league_authority > 100 }
					set_variable = { name = mycenaean_league_authority value = 100 }
				}
				if = {
					limit = { var:mycenaean_league_authority < 0 }
					set_variable = { name = mycenaean_league_authority value = 0 }
				}
			}
		}
	}
}
# subtracts untiy with the $value$ its set in the events file
subtract_mycenaean_league_authority = {
	international_organization:mycenaean_league ?= {
		custom_description = {
			text = change_mycenaean_league_authority_text
			value = $value$
			change_variable = {
				name = mycenaean_league_authority
				subtract = $value$
			}
			if = {
				limit = { has_variable = mycenaean_league_authority }
				if = {
					limit = { var:mycenaean_league_authority > 100 }
					set_variable = { name = mycenaean_league_authority value = 100 }
				}
				if = {
					limit = { var:mycenaean_league_authority < 0 }
					set_variable = { name = mycenaean_league_authority value = 0 }
				}
			}
		}
	}
}

# Converts an extraterritorial palace house into a normal country by handing
# over the locations where it already owns the dominant palace-building network.
# It also removes the new territorial kingdom from the Mycenaean League:
# autonomy is full political independence, not merely landed league membership.
turn_mycenaean_palace_into_normal_tag = {
	save_temporary_scope_as = target_palace
	custom_tooltip = {
		text = turn_mycenaean_palace_into_normal_tag.tt
		change_country_type = location
		every_owned_foreign_building_location = {
			limit = {
				mycenaean_palace_house_can_take_location = yes
			}
			change_location_owner = prev
			add_core = prev
			change_integration_level = core
			if = {
				limit = {
					NOR = {
						has_building = building_type:stockade
						has_building = building_type:castle
					}
				}
				change_building_level_in_location = {
					building = building_type:stockade
					value = 1
				}
			}
		}
		set_variable = { name = mycenaean_landed_palace value = 1 years = 30 }
		set_variable = { name = mycenaean_palace_autonomy_claimed value = 1 years = 8 }
	}
	international_organization:mycenaean_league ?= {
		remove_country_from_international_organization = scope:target_palace
	}
	reverse_add_opinion = { modifier = bronze_mycenaean_palace_autonomy target = international_organization:mycenaean_league.leader_country }
}

# Reverses the process for small palace states defeated or reconciled by the
# High Wanax: land returns to the league leader, while the old palace survives
# as an extraterritorial political house.
turn_mycenaean_palace_into_building_based = {
	save_temporary_scope_as = target_palace
	custom_tooltip = {
		text = turn_mycenaean_palace_into_building_based.tt
		every_owned_location = {
			change_location_owner = international_organization:mycenaean_league.leader_country
			add_core = international_organization:mycenaean_league.leader_country
			change_building_level_in_location = {
				building = building_type:bronze_age_palace_house
				value = 1
				owner = prev
			}
		}
		change_country_type = building
		remove_variable = mycenaean_landed_palace
		remove_variable = mycenaean_palace_autonomy_claimed
		remove_variable = mycenaean_fully_independent_palace
		if = {
			limit = { NOT = { has_variable = mycenaean_palace_influence } }
			set_variable = { name = mycenaean_palace_influence value = 0 }
		}
		change_variable = { name = mycenaean_palace_influence subtract = 20 }
		if = {
			limit = { var:mycenaean_palace_influence < 0 }
			set_variable = { name = mycenaean_palace_influence value = 0 }
		}
	}
	if = {
		limit = {
			NOT = {
				is_member_of_international_organization = international_organization:mycenaean_league
			}
		}
		international_organization:mycenaean_league ?= {
			add_country_to_international_organization = scope:target_palace
		}
	}
	add_opinion = { modifier = bronze_mycenaean_reconciled_palace target = international_organization:mycenaean_league.leader_country }
}

mycenaean_fragmentation_reevaluate_strongest_palaces = {
	international_organization:mycenaean_league ?= {
		ordered_international_organization_member = {
			order_by = {
				add = { value = country_total_army_levy_size }
				multiply = country_rank_level
				add = {
					value = army_size
					multiply = 10
				}
				add = {
					value = country_total_navy_levy_size
					divide = 2
				}
				add = {
					value = monthly_income_trade_and_tax
					divide = 10
				}
				if = {
					limit = { has_variable = mycenaean_palace_influence }
					add = var:mycenaean_palace_influence
				}
				if = {
					limit = {
						has_special_status_in_international_organization = {
							type = special_status:great_palace_candidate
							international_organization = international_organization:mycenaean_league
						}
					}
					add = 100
				}
				if = {
					limit = {
						has_special_status_in_international_organization = {
							type = special_status:palace_candidate
							international_organization = international_organization:mycenaean_league
						}
					}
					add = 50
				}
			}
			limit = {
				this != international_organization:mycenaean_league.leader_country
			}
			max = 1
			check_range_bounds = no
			situation:mycenaean_fragmentation = { # situation will be removed fix this AND saved some where else mabey IO
				set_variable = {
					name = mycenaean_fragmentation_strongest_palace
					value = prev
				}
			}
		}
		ordered_international_organization_member = {
			order_by = {
				add = { value = country_total_army_levy_size }
				multiply = country_rank_level
				add = {
					value = army_size
					multiply = 10
				}
				add = {
					value = country_total_navy_levy_size
					divide = 2
				}
				add = {
					value = monthly_income_trade_and_tax
					divide = 10
				}
				if = {
					limit = { has_variable = mycenaean_palace_influence }
					add = var:mycenaean_palace_influence
				}
			}
			limit = {
				this != international_organization:mycenaean_league.leader_country
			}
			position = 1
			check_range_bounds = no
			situation:mycenaean_fragmentation = { # situation will be removed fix this AND saved some where else mabey IO
				set_variable = {
					name = mycenaean_fragmentation_second_palace
					value = prev
				}
			}
		}
	}
}

# might be able to just but the whole on_monthly in an IO scope to fixe problems
# international_organization:mycenaean_league ?= {
# 	on_monthly = { # Can mainly be moved to effects
# 			if = {
# 				limit = { NOT = { has_variable = mycenaean_fragmentation_months } }
# 				set_variable = { name = mycenaean_fragmentation_months value = 0 }
# 			}
# 			change_variable = { name = mycenaean_fragmentation_months add = 1 }

# 			mycenaean_fragmentation_reevaluate_strongest_palaces = yes

# 			# The situation is always visible as a league management panel.
# 			# The actual collapse logic begins only once authority or wider crisis
# 			# pressure makes fragmentation historically plausible.
# 			if = {
# 				limit = {
# 					NOT = { has_variable = mycenaean_fragmentation_crisis_active } 
# 					# Six-year grace period: the league panel is active from day one,
# 					# but palace fragmentation cannot begin before members have had
# 					# time to develop.
# 					var:mycenaean_fragmentation_months >= 72
# 					international_organization:mycenaean_league = {
# 						any_international_organization_member = {
# 							mycenaean_palace_is_valid_landable_member = yes
# 						}
# 						OR = {
# 							var:mycenaean_league_authority <= 8
# 							AND = {
# 								var:mycenaean_league_authority <= 25
# 								leader_country ?= {
# 									OR = {
# 										government_power <= 35
# 										stability <= 15
# 										in_civil_war = yes
# 										has_variable = mycenaean_palaces_burning
# 									}
# 								}
# 							}
# 							AND = {
# 								var:mycenaean_league_authority <= 35
# 								is_situation_active = situation:sea_peoples_crisis
# 								situation:sea_peoples_crisis ?= {
# 									has_variable = sea_peoples_global_phase
# 									var:sea_peoples_global_phase >= 3
# 								}
# 							}
# 						}
# 					}
# 				}
# 				set_variable = { name = mycenaean_fragmentation_crisis_active value = 1 } # this one is might need fixing if moved Here it set so might not need fixing
# 				international_organization:mycenaean_league = {
# 					ordered_international_organization_member = {
# 						limit = { mycenaean_palace_is_fragmentation_vanguard = yes }
# 						max = 12
# 						check_range_bounds = no
# 						order_by = total_foreign_buildings_levels
# 						turn_mycenaean_palace_into_normal_tag = yes
# 					}
# 					mycenaean_fragmentation_reevaluate_strongest_palaces = yes
# 					every_international_organization_member = {
# 						trigger_event_non_silently = mycenaean_league.200
# 					}
# 				}
# 			}

# 			if = {
# 				limit = {
# 					has_variable = mycenaean_fragmentation_crisis_active 
# 					international_organization:mycenaean_league = {
# 						var:mycenaean_league_authority >= 45
# 						NOT = {
# 							any_international_organization_member = {
# 								mycenaean_palace_is_valid_landable_member = yes
# 							}
# 						}
# 					}
# 				}
# 				remove_variable = mycenaean_fragmentation_crisis_active 
# 				international_organization:mycenaean_league ?= {
# 					every_international_organization_member = {
# 						trigger_event_non_silently = mycenaean_league.250
# 					}
# 				}
# 			}

# 			if = {
# 				limit = {
# 					has_variable = mycenaean_fragmentation_crisis_active 
# 				}
# 				random_list = {
# 					20 = {
# 						trigger = {
# 							international_organization:mycenaean_league = {
# 								any_international_organization_member = {
# 									mycenaean_palace_is_valid_landable_member = yes
# 									OR = {
# 										var:mycenaean_palace_influence >= 15
# 										international_organization:mycenaean_league = {
# 											var:mycenaean_league_authority <= 10
# 										}
# 									}
# 								}
# 							}
# 						}
# 						international_organization:mycenaean_league = {
# 							random_international_organization_member = {
# 								limit = {
# 									mycenaean_palace_is_valid_landable_member = yes
# 									OR = {
# 										var:mycenaean_palace_influence >= 15
# 										international_organization:mycenaean_league = {
# 											var:mycenaean_league_authority <= 10
# 										}
# 									}
# 								}
# 								trigger_event_non_silently = mycenaean_league.210
# 							}
# 						}
# 					}
# 					12 = {
# 						trigger = {
# 							exists = international_organization:mycenaean_league.leader_country
# 						}
# 						international_organization:mycenaean_league.leader_country = {
# 							trigger_event_non_silently = mycenaean_league.220
# 						}
# 					}
# 					220 = { }
# 				}
# 			}
# 		}
#     }	